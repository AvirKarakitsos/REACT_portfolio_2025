export type ThemeContextType = {
    theme: string;
    toggleTheme: (input: string) => void;
};

export type LanguageContextType = {
    lang: string;
    toggleLanguage: (input: string) => void;
 };